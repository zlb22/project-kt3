# Production Environment Configuration
SECRET_KEY=your-production-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=project-kt3

# Production CORS - restrict to your domain
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Upload Configuration
UPLOAD_DIR=uploads

# MinIO Configuration
MINIO_URL=https://yourdomain.com:9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=your-production-minio-secret
MINIO_BUCKET_KETI3=online-experiment

# Production: Use Nginx proxy path
KETI3_FRONTEND_URL=https://yourdomain.com/topic-three/online-experiment

MINIO_BASE_PATH_KETI3=creative_work
MINIO_ALLOWED_BUCKETS=onlineclass,another-bucket

# Keti3 API Configuration
APP_ID=keti-3
SKEY=your-production-skey
MYSQL_DSN=mysql+pymysql://root:password@localhost:3306/kt3
MINIO_BASE_PATH=24game

# Disable debug logging in production
UPLOADS_JSON_LOG_ENABLED=false
