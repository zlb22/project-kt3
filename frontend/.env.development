# Expose CRA dev server on all interfaces and port 3000
HOST=0.0.0.0
PORT=3000

# Disable host header check for intranet/IP access (development only)
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Improve HMR/WebSocket compatibility when accessed via IP
WDS_SOCKET_HOST=0.0.0.0
WDS_SOCKET_PORT=0
